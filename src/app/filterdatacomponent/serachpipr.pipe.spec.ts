import { SerachpiprPipe } from './serachpipr.pipe';

describe('SerachpiprPipe', () => {
  it('create an instance', () => {
    const pipe = new SerachpiprPipe();
    expect(pipe).toBeTruthy();
  });
});
